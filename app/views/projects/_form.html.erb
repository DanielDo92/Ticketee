<%= simple_form_for(project) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %><br>

  <%= f.button :submit, class: "btn-primary" %> <br><br>
  <!--#For learning purpose only-->
  <br>
  <%= f.submit :Clear, type: :reset, class: "btn-info" %>
  <%= f.submit :Cancel, name: :cancel%>
<% end %>